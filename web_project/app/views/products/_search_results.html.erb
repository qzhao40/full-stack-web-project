<h2>Search Results</h2>

<% if @products.size.zero? %>
 <p>No results found</p>

<%else%>
<%= render partial: 'products/product',
    collection: @products
    locals: {
      display_link_to_show: true,
      display_link_to_edit: false}%>
